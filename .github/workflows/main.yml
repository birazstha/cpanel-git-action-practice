name: Deploy Laravel Project on Push

on:
  push:
    branches:
      - master

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
     
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.rajphotosspasal.com
          username: practice@ss.rajphotopasal.com
          password: Biraj@224ss973
  
